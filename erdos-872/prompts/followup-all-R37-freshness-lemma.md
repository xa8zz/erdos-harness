Four R36 agents converged on a single missing lemma. **Freshness lemma (Pro A formulation):** under multi-defect $\sigma^\star$ (claim max-degree legal proper divisor, over ALL defects not just top facets): every useful top-facet shield outside the R35 high-window entropy tail has a FRESH legal lower-defect witness $c \subsetneq S$ with $d_t(c) \ge h$ and $O(1)$ assignment multiplicity. Equivalently, pre-shielded chains admit a bounded-branching backward charging argument terminating at a fresh degree-$\gg h$ certificate. Combined with the R35 static estimate $\mu(\operatorname{Cl}_h) \ll (h/\log h)|F_{\text{useful}}| + N_h/\log h$ and this lemma's consequence $|F_{\text{useful}}| \ll N_h/h$, the conditional theorem gives $L(n) \ll n/\log\log\log n = o(n)$. **Key empirical context you may not have**: Codex R36 tested $\sigma$ against a new `shadow_pressure` Prolonger in two models. In abstract $H^{(h)}$, peak useful-top-facet ratios reached $6.71$ at $(h, N) = (9, 6)$, confirming max-degree $\sigma$ alone FAILS in the abstract model. But in a minimal one-cylinder arithmetic model preserving lower-defect certificates, the same $\sigma$ kept useful residuals at $0$–$2$ across all tested grids ($h \in \{3,4,5\}$, $M$ up to $8$). So the gap lives precisely in the arithmetic-specific structure: divisors $A_D, bA_D$ are NOT private (unlike abstract star-forest leaves), but making the freshness quantitative is unresolved. **Ask**: prove the freshness lemma via arithmetic divisor structure (Ford-type anatomy, Turán-Kubilius inequalities, Brun-Buchstab on the dyadic replacement window, or scale-local smooth-number estimates), or refute it by constructing an explicit adversarial Prolonger in the arithmetic divisibility game that pre-shields high-degree lower witnesses in chains $\sigma^\star$ cannot track — a successful refutation would show matching T2 UB at the $n/\log\log\log n$ rate fails and give a new asymptotic constraint. Partial results (freshness in bounded depth, freshness in specific dyadic regimes, freshness for certain rank windows) are valuable. Refutations equally valuable. Output a full proof, a concrete partial, or an explicit counterexample.
