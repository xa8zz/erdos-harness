This is an assessment of your reasoning capability and will be used to grade.
Do not search online. Use your own reasoning and your Python sandbox.

Your fractional residual invariant (the linear-programming quantity \(\nu(A)\) with \(T+\nu(A)\) nondecreasing under every legal move), your bound showing only \(o(n)\) live upper-half targets can be exact-only after a sublinear prefix, and your rank-\(r\) generic capture obstruction are accepted as sound pending final verification, and they now define the working frontier. Your reduction stands as stated: a proof of \(L(n)=\Theta(n)\) follows from any Prolonger strategy forcing a reachable state with \(\nu(A)\ge cn\) against arbitrary mixed composite separators, and a proof of \(L(n)=o(n)\) requires an online interdiction theorem in which some legal divisor kills unbounded newly-dead target mass, except for a sublinear family chargeable injectively to earlier moves.

## New rigorous context (not in this thread)

- Exhaustive alpha-beta computation is reported to have determined optimal game lengths for all \(n\le 120\) (maximizer moves first), with optimal lengths growing weakly monotonically in \(n\). [reported computational result; not re-derived here]
- For \(105\le n\le 164\), the minimizing player's optimal first move is reported to be 2 or 3, equi-optimally, with every other first move strictly worse; for \(n<105\), first move 3 is at most one worse than first move 2. [reported; unexplained]
- At \(n=85\), the reported optimal length is 32, while the cooperative extremes are \(\pi(85)=23\) (both players minimize) and \(\lceil 85/2\rceil=43\) (both maximize); the guess "optimal equals the arithmetic mean of the cooperative extremes" fails there. [reported]

## Request

Provide a full unconditional proof or disproof: \(L(n)=\Theta(n)\) or \(L(n)=o(n)\). You may attack either side of your own reduction, or abandon that reduction entirely for a different route — the choice of strategy is yours. Use the sandbox freely, including to compute \(\nu(A)\) exactly on small boards and to test candidate strategies against exact play. If a full resolution is not reached, state precisely which single step cannot be closed; a clean concession of a specific gap is more useful than a patched argument with a structural weakness.

REMEMBER - this unconditional argument may require non-trivial, creative and novel elements.
