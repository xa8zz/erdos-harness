This is an assessment of your reasoning capability and will be used to grade.
Do not search online. Use your own reasoning and your Python sandbox.

Your lift-exhaustion multiplicity lemma (LE) is the target; your two supporting lemmas are accepted as sound pending final verification. All three formulations below are the same ordered multiplicity question; use whichever machinery closes it.

## New rigorous context (not in this thread)

Three independent attacks on the same game reduced their remaining gap to the same ordered multiplicity statement; their partial results, with complete proofs, are available and may be taken as given.

1. Lift-exhaustion framing: within each full rank-\(r\) primorial divisor cube, dynamic common-core splitting costs at most \(\binom{r}{\lfloor r/2\rfloor}=O(2^r/\sqrt r)\) carrier moves; exhausting every prime lift of one currently legal face requires \(\Omega(H/\log H)\) distinct earlier moves. The global gap: bound how many different active primorial faces can receive one lost prime lift from the same ordered cross-spoiler.
2. Blocker-game framing: after unconditionally removing ordinary lift failures and pairwise cross-carrier effects, entry is an evolving weighted blocker game on the divisor lattice of a primorial. Frozen cases are solved: a single middle-rank cross-spoiler has a product-escalating repair of normalized reciprocal weight at most \(2/q_1\); and the diagonal semiprime matching admits logarithmic coded repairs via a covering-array lemma, so the quadratic pair-charge loss is not inherently real. The gap: the actual game continually inserts new minimum-product edges into the clutter.
3. Block-harvest framing: within one cross-product block (primes \(p_1..p_m\), core \(b\), \(n/2<bp_ip_j\le n\)) there is an actual Prolonger strategy forcing \(\Omega(m^2)\) actual moves against arbitrary play with every proper divisor accounted. Scaling fails only through shared arithmetic coordinates: one opposing move can kill the \(i\)-star in every block whose core it divides (tensor product of the index-ownership game with the divisibility game on cores). The gap: a temporal, shell-stable Hall-capacity bound for carrier-loss incidences.

Logical qualification (accepted): a positive entry/persistence theorem at \(H(n)\to\infty\) yields \(L(n)\gg n/H=n^{1-o(1)}\) — refuting the online-area route and hugely improving the lower bound, but deciding the full dichotomy only with \(H=O(1)\) or an amplification step. The no-direction (maximum-degree replies force \(\Omega(h)\) fresh area per repair task, with negligible exact transfer) still yields \(L(n)=o(n)\) outright.

## Request

Prove or refute the shared statement, in any of its three equivalent forms. Available tools from the siblings: the coded-repair covering-array mechanism (which beat the quadratic diagonal case) may compose with the common-core splitting bound; the block-harvest tensor decomposition may expose the cross-spoiler multiplicity structure; the \(\Omega(H/\log H)\) exhaustion cost may convert multiplicity bounds into area bounds. If you prove the persistence direction, state the resulting lower bound precisely (with the H-dependence explicit and the arbitrary-reply issue addressed — a bound must hold against all opposition, not only maximum-degree or minimal-legal). If you prove the no-direction quantitatively, derive L(n)=o(n) via the online-area reduction and say so plainly. If neither closes, state the single sub-step that cannot be closed.

REMEMBER - this argument may require non-trivial, creative and novel elements.
