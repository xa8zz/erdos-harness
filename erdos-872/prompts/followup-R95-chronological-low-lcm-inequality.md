This is an assessment of your reasoning capability and will be used to grade.
Do not search online. Use your own reasoning and your Python sandbox.

Your advance-depletion reduction is accepted as sound pending final verification: Theorem 1 (fresh-batch persistence at \(2W_r+1\) lifts per face), Theorem 2 (all-orders witness inequality (8)), the high-lcm truncation (11), and the isolation of the chronological inequality (TL) as the exact separator of the two outcomes.

## New rigorous context (not in this thread)

A claimed policy-independent persistence theorem at maximal-divisor scale (temporal face capacity of one ordered spoiler at most \(\binom{r}{\lfloor r/2\rfloor}\mathfrak T(n)\), where \(\mathfrak T(n)\) is the maximal divisor count on the board) was refuted by two independent audits with the same mechanism, realized concretely: an instantaneous Sperner width bound was silently promoted to a lifetime capacity, but one old spoiler is inherited by descendants across refinement stages — over one core, sequential Boolean stages reveal \(2^r\) core-signature pairs while every instant shows at most \(\binom{r}{\lfloor r/2\rfloor}\). Realized at \(n=4000\) with \(c=30\): lifetime multiplicity 128 versus instantaneous 48, on a 258-move verified-antichain history. A repair would need a canonical splitter with a historical no-repetition lemma (any factor \(H^{o(1)}\) suffices arithmetically); none is known. In your language this inheritance is exactly a low-lcm multiplicity event: the witnesses share one small core \(e\), so every \(s\)-tuple has \(L_I=e\).

## Request

Prove or refute (TL) — the chronological low-lcm capacity inequality — in your weighted block version. A proof gives the online-area statement and \(L(n)=o(n)\) outright via your own reduction; state the resulting theorem plainly. A refutation must construct the ordered history at (14): \(t=o(X/H)\), \(\mathsf{Area}_t=o(X)\), \(\mathsf{Bank}_t=o(X)\), a positive-weight family of untouched batches with \(q=(1-o(1))m\) tags blocked in advance, full-order low-lcm sum, and every coded or product-escalating repair legally cross-spoiled in chronological order — the inheritance mechanism above is a potential source for the last line that the static endpoint lacked. Either outcome resolves or decisively reroutes the dichotomy; do not force one. If neither closes, state the single sub-step that cannot be closed.

REMEMBER - this argument may require non-trivial, creative and novel elements.
