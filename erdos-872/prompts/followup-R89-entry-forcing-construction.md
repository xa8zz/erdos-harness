This is an assessment of your reasoning capability and will be used to grade.
Do not search online. Use your own reasoning and your Python sandbox.

Your absorbing-fortress theorem is accepted as sound pending final verification: post-entry persistence is automatic, forced collapse is impossible, and the only missing event is robust entry against cross-spoiling replies.

## New rigorous context (not in this thread)

- Online-area reduction: \(L(n)=o(n)\) follows from one statement (OA): along any play under the maximum-degree Shortener policy, processed shell by shell from the top, each dyadic shell \(S_X\) is cleared in \(O(X\log h/h)\) Shortener turns. Equivalently, the single unresolved question is the ordered primorial persistence problem: after the trapping sweep, can Prolonger legally order and continually refresh primorial-batched or cross-carrier moves against the actual maximum-degree Shortener policy so that for some shell \(S_X\) and \(h(n)\to\infty\), \(t\asymp X/h(n)\) rounds keep \(|Q_i|\gg X\) while \(\sum_{i<t}\Delta_i=o(X)\) and only \(o(X)\) targets are killed by Prolonger or transferred to the minimal legal set. There is also an unconditional shell-stable PAIR charge (bounded multiplicity to carrier pairs, capacity \(X/2s\)), which is quadratically too weak on batched positions.
- Ancestor-collapse lemma: under the minimal-legal Shortener policy (always play a legal element with no legal proper divisor; one always exists by divisor descent), Shortener's own moves never regenerate surviving upper targets and equal the minimal legal completion at termination; all regeneration accounting collapses onto Prolonger's carriers alone. A 5n/24-cover construction shows this reduction alone still permits linearly many completions.

## Request

Force entry, or prove it impossible. Either: (a) construct an explicit Prolonger entry strategy — an ordering, batching schedule, and repair rule for cross-spoiled carriers — that reaches a primorial-batched position with surviving cap \(H\) (any \(H=n^{o(1)}\); slower is stronger) against every opposing policy, with the cross-spoiling repair cost accounted; by your fortress theorem this immediately yields \(L(n)\ge \varepsilon n/H\). Or: (b) prove the entry race is lost — the cumulative maximum-degree area consumed while building any batched trapped state is \(\Omega(X)\) per shell (the ordered-persistence no-direction), which by the online-area reduction gives \(L(n)=o(n)\). Sandbox the entry race at moderate n first. Either outcome resolves the asymptotic dichotomy. If neither closes, state the single sub-step that cannot be closed.

REMEMBER - this argument may require non-trivial, creative and novel elements.
