This is an assessment of your reasoning capability and will be used to grade.
Do not search online. Use your own reasoning and your Python sandbox.

Your disproof of the fixed-budget charging inequality and your identification of the dynamic exact-only bank are accepted as sound pending final verification. Your sharply stated remaining sub-step is now the frontier: for trapped positions arising under an actual Shortener strategy, a bounded-multiplicity, shell-stable charge for \(Q(A)=R(A)\setminus E(A)\) — e.g. your (33) — with exact-only transfers paid from the cumulative exact-move bank.

## New rigorous context (not in this thread)

An independent disproof of the same fixed-budget inequality was found by a different construction; take it as given. Primorial batching: for every \(b(n)=o(n)\) and fixed \(C\) there is a reachable position \(A_n\) with \(|R(A_n)|=(\tfrac12-o(1))n\) — a linear live reservoir — and \(\Delta(A_n)\le H_n\) for a slowly growing \(H_n\) with \((|A_n|+b(n))H_n=o(n)\). A sublinear family of upper-half primorial-batched carriers simultaneously makes every lower integer up to \(n/H\) illegal without killing any other upper target; every surviving legal lower divisor exceeds \(n/H\) and so has degree below \(H\). Note this position has essentially no exact-only targets: the reservoir survives with all its small-prime divisors blocked only from below, so the exact-only bank does not obviously pay for it.

## Request

Prove or disprove your remaining sub-step: a bounded-multiplicity, shell-stable charge for the non-exact trapped residual family, with the exact-only transfer term paid by the bank — in a form strong enough that a maximum-degree Shortener strategy plus multiplicative decrease yields \(L(n)=o(n)\). Test any candidate charge against BOTH counterexample mechanisms: your face-carrier diagonalization and the primorial-batching position above (where \(\Delta\) is capped at \(H\) while the reservoir is linear — a valid charge must either show such positions cannot persist under the actual Shortener strategy, or pay for them another way). A disproof means showing every such charge fails on positions reachable against the intended strategy, with the mechanism identified. If neither is reached, state precisely the single sub-step that cannot be closed.

REMEMBER - this argument may require non-trivial, creative and novel elements.
