# Codex Task A: Paper-Wide Polish + Rigor Repairs

Round-3 Pro audits (rigor + consistency + reader + weakest-link) returned
with no FATAL gaps but twenty small/medium repair items spread across the
paper. Integrate one commit per item. The three Section 8 barrier repairs
(R1k, R1l, R1m) are NOT in this task — they go to Dispatch B.

Working directory: `/Users/omisverycool/erdos-harness`.

## Compile / workflow rules

- Rebuild with `tectonic -X compile main.tex` from `erdos-872/paper/`.
- One commit per fix; do not batch.
- No amend, no force-push, no `--no-verify`.
- Push after all commits with `git push origin HEAD`.

## Task group 1 — Citation hygiene (3 items, SHIP-BLOCKING)

### A1.1 — Remove `% TODO: independently verify` comment from references.bib

The entry `Er92c` currently has a `% TODO` comment above it flagging
that the citation is a best-effort guess. This comment cannot remain in
the submitted bibliography. Remove it. If the citation is still
unverified, leave the entry and the committer/author verifies separately
before arXiv upload.

Commit message: `Paper: remove TODO comment from Er92c bibliography entry`.

### A1.2 — Fix `PriceGPTPro26` citation

Current entry lists `author = {Price, Liam and {GPT-5.2 Pro}}`. Standard
academic practice is that AI systems are not bibliographic authors.
Change to:

```bibtex
@misc{PriceGPTPro26,
  author = {Price, Liam},
  title = {First linear upper bound for Erd\H{o}s problem \#872},
  year = {2026},
  howpublished = {\url{https://www.erdosproblems.com/forum/thread/872}},
  note = {Forum contribution; the author disclosed AI-assisted
    derivation in the post itself.}
}
```

The paper's own AI Declaration section already covers the AI
attribution; moving GPT-5.2 Pro out of the bibliographic author field
is pure hygiene, not erasure.

Commit message: `Paper: remove AI coauthor from PriceGPTPro26 citation`.

### A1.3 — Add GitHub commit hash to `ErdosHarness26`

Current entry points to `https://github.com/xa8zz/erdos-harness` without
a specific commit. For arXiv reproducibility, the citation should pin to
a specific commit. Update the `howpublished` field:

```bibtex
howpublished = {\url{https://github.com/xa8zz/erdos-harness}, commit
  \texttt{[COMMIT_HASH]}},
```

Use `git rev-parse HEAD` to get the current `main` hash and substitute.
Also update the `note` field to include "accessed and snapshot at
commit \texttt{[COMMIT_HASH]}, 2026-04-21."

Commit message: `Paper: pin ErdosHarness26 citation to current main commit`.

## Task group 2 — Cross-section consistency (7 items)

### A2.1 — Notation drift `L(P)` → `L_n(P)` in introduction

The introduction defines `\mathcal L(P) := \{x \le n/2 : x \nmid u\
\text{for every}\ u \in P\}` before `thm:intro-A`. The body (including
`sec:notation` and `sec:shield`) uses `\mathcal L_n(P)`. Rename the
introduction occurrence to `\mathcal L_n(P)` for consistency.

Commit message: `Paper: align L(P) notation to L_n(P) in introduction`.

### A2.2 — First-hit cover domain in introduction

The introduction's `\tau(n)` definition uses "for all $x \le n/2$". The
body uses "$x \in L_n$". Use `$x \in L_n$` in the introduction.

Commit message: `Paper: align first-hit cover domain to x in L_n in introduction`.

### A2.3 — First-hit cover intro/body statement alignment

The introduction's `thm:intro-cover` states both `\tau(n) = 5n/24 +
O(1)` AND the explicit cover $H_n$ + matching packing. The body's
`thm:five-twenty-four` only states the asymptotic equality; the
explicit cover appears in the proof.

Choice: align the body theorem statement to include the explicit cover
and packing existence. Rewrite `thm:five-twenty-four` statement to
mirror the introduction's statement exactly.

Commit message: `Paper: align body 5/24 theorem statement to include explicit cover + packing`.

### A2.4 — Intro shield theorem split

The introduction's `thm:intro-shield` combines two body results: the
inequality `|A| \ge |\U_n| - \beta_n(P)` (body `thm:shield-reduction`)
and the prefix-size obstruction `|P| \ge n^{e^{-2c} - o(1)}` (body
`cor:shield-obstruction`, dependent on Theorem A).

Choice: split the introduction's theorem into two. Use
`thm:intro-shield` for the reduction only, and add
`cor:intro-shield-obstruction` for the prefix-size consequence. Match
the body structure.

Commit message: `Paper: split intro shield theorem into theorem + corollary to match body`.

### A2.5 — T2 conditional cross-reference correction

The introduction's conditional T2 paragraph says the restricted
hypothesis is for states generated by "the activation and residual
strategies in \Cref{sec:lower-bounds}". But `sec:lower-bounds` gives
only the roadmap; the actual strategies are constructed in
`app:deferred-proofs`. Change the cross-reference to point to
`app:deferred-proofs`, or cite both sections.

Commit message: `Paper: correct T2 conditional cross-reference to point to Appendix A`.

### A2.6 — 5/16 formalization description alignment

The introduction's formalization paragraph says "Theorem A and the 5/16
upper bound have Lean artifacts with remaining sorries that are standard
analytic-number-theory inputs or their assembly." Appendix C's row for
`thm:five-sixteen` describes the remaining sorry as covering BOTH the
Chebyshev prime-prefix estimate AND the "game-tree wrapper realizing the
prescribed prefix." The latter is not purely analytic-number-theory.

Adjust the introduction sentence to: "Theorem A and the 5/16 upper
bound have Lean artifacts with remaining sorries that are standard
analytic-number-theory inputs or their assembly; the 5/16 artifact's
remaining sorry also covers the game-tree wrapper realizing the
prescribed prefix."

Commit message: `Paper: clarify 5/16 formalization remaining sorries include game-tree wrapper`.

### A2.7 — Main-upper formalization remark wording

The remark after `thm:main-upper` says Theorems `thm:bonf-comp` and
`thm:prime-bridge` "supply those moment hypotheses." But
`thm:prime-bridge` supplies the moment convergence, while
`thm:bonf-comp` CONSUMES moment convergence and converts it to the
Bonferroni expansion.

Reword: "Theorem `thm:prime-bridge` supplies the moment convergence
from the envelope sequence to actual primes, and Theorem
`thm:bonf-comp` applies the fourth-order Bonferroni comparison to the
resulting prime sequence."

Commit message: `Paper: clarify main-upper formalization remark assigns moment convergence to prime-bridge`.

### A2.8 — Rename bare `U` for slot set in K_4 refutation

In the `prop:t2-safe-edge-refutation` proof in
`appendix-A-deferred-proofs.tex`, the set `U := \{q, 13q, 17q, 19q,
23q\}` clashes with the global upper-half `\U_n` notation. Rename the
local set to `U_{\mathrm{fib}}` (or similar) to avoid confusion.

Commit message: `Paper: rename local slot set U to U_fib in K_4 refutation`.

### A2.9 — Rename `A_0` scalar in 5/16 proof

The 5/16 proof uses `A_0 > 2` as a real parameter. The fan lower-bound
proof uses `A_0` as a set. Different types, same symbol, far apart but
visible. Rename the 5/16 parameter to `A_{\star}` or `C_0`.

Commit message: `Paper: rename A_0 scalar parameter in 5/16 proof to avoid type clash`.

## Task group 3 — Rigor repairs (10 items, from Pro audit #1)

### A3.1 — Local prime-count lemma terminal-time clause (R1a)

`lem:prime-count-per-range` has a proof that invokes a "terminal
time" alternative when the first odd-prime move exceeding X never
occurs. Pro's audit says the resulting inequality
`M \le S(X) + h(S(X) + 1)` does not literally follow if many composite
moves happen after prime play ends.

Fix: restate the lemma without the terminal-time alternative, OR add
an explicit hypothesis that the stopping time occurs before any
non-prime fallback. The later application in `prop:local-density`
already satisfies this hypothesis (long prime prefix exists, $X \le
n^{1 - \tau_H}$), so the fix is wording, not structure.

Commit message: `Paper: repair local prime-count lemma terminal-time clause`.

### A3.2 — 13/36 theorem explicit ε↓0 (R1b)

The proof of `thm:thirteen-thirtysix` obtains $q_j \le (3/2 +
\varepsilon) j \log n$, then claims $\sum_{j \le k} 1/q_j \ge 1/3 -
o(1)$. Pro's audit: the immediate bound is $1/(3 + 2\varepsilon) +
o(1)$, not $1/3 - o(1)$. The $1/3$ conclusion follows only after
letting $\varepsilon \downarrow 0$ (which is valid for a lower bound,
since the strategy works for every $\varepsilon > 0$).

Add the explicit step: "Let $\varepsilon \downarrow 0$ after $n \to
\infty$; the lower bound $1/3 - o(1)$ follows because the strategy
works for every $\varepsilon > 0$."

The same pattern appears mildly in the 5/16 proof; add similar
clarification if applicable.

Commit message: `Paper: add explicit epsilon downarrow 0 step in 13/36 and 5/16 proofs`.

### A3.3 — Interval-arithmetic certificate artifact reference (R1c)

Pro's audit: the interval-arithmetic certification in `prop:wfour-
certification` cites "outward-rounded interval arithmetic" without a
reproducible transcript. For a narrow-margin numerical theorem, a
certificate file or reproducible script must be cited.

Add to the appendix a sentence pointing to the artifact repository:
"A reference Python implementation using \texttt{mpmath} with explicit
directed-rounding modes is available at
\texttt{scripts/wfour\_certification.py} in the artifact repository
\cite{ErdosHarness26}; running it with the default parameters
reproduces the certified intervals displayed above."

Optionally (if easy): create the actual script
`scripts/wfour_certification.py` implementing the certification.

Commit message: `Paper: cite reproducible interval-arithmetic script in W_4 certification`.

### A3.4 — Formalization claims precision (R1d)

Pro's audit: the paper repeatedly says "zero-sorry Lean-verified" but
no Lean file paths, theorem names, commit hashes, or Lean versions
appear in the paper source. A formalization claim must be externally
verifiable.

Update Appendix C to include for each row: (i) exact file path within
the artifact repository; (ii) exact theorem declaration name; (iii)
commit hash at which the verification was last run; (iv) the Lean
toolchain version. The introduction's formalization paragraph should
reference Appendix C for these specifics rather than asserting
"verified" in the body.

If any of (i)-(iv) are unknown, leave a placeholder `[TBD]` and commit
with a clear note that these must be filled before arXiv upload.

Commit message: `Paper: add Lean file paths, theorem names, and commit hash to Appendix C`.

### A3.5 — T2 roadmap proof → precise pointer (R1e)

`thm:t2` currently closes its proof environment with a roadmap rather
than a proof. A theorem cannot close by roadmap unless it cites the
actual proof immediately and unambiguously.

Change the closing line to: "Proof: see `prop:t2-final` in
`app:deferred-proofs`; the full proof assembles
`prop:t2-activation-potential`, `prop:t2-deletion-budget`,
`prop:t2-residual-mass`, and `prop:t2-residual-embedding` under the
restricted safe-edge hypothesis of `def:safe-edge`."

Ensure `prop:t2-final`'s statement matches `thm:t2`'s conclusion
exactly.

Commit message: `Paper: replace T2 roadmap proof with precise pointer to Appendix A`.

### A3.6 — K_4 fiber reachability clarification (R1f)

`prop:t2-safe-edge-refutation` constructs the counterexample state by a
cooperative sequence of Maker and Breaker choices. "Reachable" here
means "reachable under SOME strategy pair," not "reachable under the
specific activation/residual strategies of the conditional theorem."

Add a sentence at the end of the proposition's statement or the start
of its proof: "Here 'reachable' means reachable under some legal
play sequence, not necessarily under the specific Prolonger strategies
of `prop:t2-final`. The construction refutes the universal safe-edge
hypothesis over the full state space; the main theorem's restricted
hypothesis (`def:safe-edge`) is formulated for states generated by
Prolonger's specific strategies, not over the full state space."

Commit message: `Paper: clarify K_4 refutation reachability scope`.

### A3.7 — Residual embedding false sentence rewrite (R1g)

In the proof of `prop:t2-final` (or wherever the residual-embedding is
used downstream), the sentence "modeled activation replies ... do not
produce comparable integers" is literally false: playing `a` or `ac`
IS comparable with targets containing that factor.

Rewrite: "Modeled activation replies produce integers that may be
comparable with some residual targets in $\mathcal T_*$; any such
comparable target has already been removed from $\mathcal T_*$ by the
residual construction. Hence no live hyperedge in the rank-3
hypergraph corresponds to a target killed by a modeled reply."

Commit message: `Paper: rewrite residual-embedding comparable-integers sentence`.

### A3.8 — Activation-stage model-to-game identification paragraph (R1h)

`prop:t2-activation-potential` applies `prop:t2-finite-capture` to the
activation game, but the mapping between the activation game's
dynamically-updated edge weight $w_t(e)$ and the scaled rank-2 graph
game of `prop:t2-finite-capture` is compressed.

Add a short paragraph before or inside the proof explicitly defining
the graph-game edge weights at each activation state, specifying that
the safe-edge hypothesis is applied to the dynamically updated weight
function $w_t(e)$, and proving that claiming an edge with one consumed
token is exactly the scaled rank-2 potential transition. The displayed
coefficient calculation in the current proof is correct; the addition
is formal identification.

Commit message: `Paper: add model-to-game identification paragraph to activation-stage potential`.

### A3.9 — Disjoint-carrier prior-multiple invariant (R1i)

`prop:disjoint-carriers` (formerly "disjoint small-prime carriers")
invokes a "skipped-move" argument when a planned semiprime is illegal.
Pro's audit: illegality can come from a prior MULTIPLE of the planned
move, not only a prior divisor.

Under the disjoint-support restriction, such prior multiples are either
impossible or harmless, but the proof must say so explicitly. Add an
invariant: "Under the disjoint-support hypothesis, no prior composite
Prolonger move is a multiple of a planned Shortener move, because such
a multiple would share at least one small-prime factor with the
Shortener move, contradicting disjointness."

Commit message: `Paper: add prior-multiple invariant to disjoint-carrier proposition`.

### A3.10 — Squarefree rank-3 skipped-move invariant (R1j)

`prop:rank-three-carriers` has the same skipped-move issue as A3.9:
"some earlier played divisor blocks it" is not justified when
illegality might come from a prior MULTIPLE.

Add the analogous invariant: "Under the squarefree rank-$\le 3$
restriction, any earlier played move that makes a planned square,
semiprime, or triple illegal is either a proper divisor of it or the
listed object itself. Proper multiples are ruled out by rank-$\le 3$."

Commit message: `Paper: add skipped-move invariant to squarefree rank-3 proposition`.

## Final self-audit

After twenty commits land and the paper recompiles cleanly, write a
self-audit (under 250 words) covering:

1. Which fixes closed fully.
2. Any commit where you needed to deviate from the stated spec and why.
3. For A3.4 (formalization claims): which of (i)-(iv) you filled in
   vs left as `[TBD]`.
4. Any new gaps you noticed while writing.
5. Compile warnings summary (should be only pre-existing underfulls).
