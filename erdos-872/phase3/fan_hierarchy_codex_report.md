# Codex Phase 3c handoff report (as delivered to curator)

Implemented the Phase 3c audit in [fan_hierarchy.py](/Users/omisverycool/erdos-harness/erdos-872/phase3/fan_hierarchy.py), with outputs in [fan_hierarchy_probeA.csv](/Users/omisverycool/erdos-harness/erdos-872/phase3/fan_hierarchy_probeA.csv), [fan_hierarchy_probeB.csv](/Users/omisverycool/erdos-harness/erdos-872/phase3/fan_hierarchy_probeB.csv), [fan_hierarchy_probeC.csv](/Users/omisverycool/erdos-harness/erdos-872/phase3/fan_hierarchy_probeC.csv), [fan_hierarchy_summary.md](/Users/omisverycool/erdos-harness/erdos-872/phase3/fan_hierarchy_summary.md), [test_fan_hierarchy.py](/Users/omisverycool/erdos-harness/erdos-872/phase3/test_fan_hierarchy.py), and [fan_hierarchy.log](/Users/omisverycool/erdos-harness/erdos-872/phase3/logs/fan_hierarchy.log). I also recorded the durable Phase 3c lesson in [AGENTS.md](/Users/omisverycool/erdos-harness/AGENTS.md).

Top-line result: this grid does not support the “all three signals positive” story. Probe A found no evidence that the current per-rank bound is very loose; the best looseness ratio was only `0.65`, and the concrete fixed-rank fan policy actually underperformed the stated low-rank reference constants. Probe B was mixed: rank-1 cross-rank shielding is real (`cross/same = 0.62` at `10^5`, `0.82` at `10^6`), but it mostly disappears by higher ranks (`0.00` at `10^5, h=2` and `0.06` at `10^6, h=2`). Probe C was the positive one: the `2σ` window around the discrete peak rank captures about `19%` of `(n/2, n]` across `10^5, 10^6, 10^7`, so there is stable linear-scale target mass, but not anything close to saturating the upper half.

One notable empirical correction to the prompt assumptions: the actual `H = sum_{p <= n^0.45} 1/p` values on this grid are `1.917`, `2.097`, and `2.245`, not roughly `2.96` and `3.11`, so the peak stays at `h* = 2` throughout. Also, some requested rows were skipped exactly where the construction degenerates: Probe A omitted ranks with `|A| < h`, and `max-degree-any` at `n = 10^6` was skipped for runtime, as allowed.

Verification: `python3 -m unittest /Users/omisverycool/erdos-harness/erdos-872/phase3/test_fan_hierarchy.py` passed. I also verified all requested artifacts exist, and the CSV row counts are `18` for Probe A, `5` for Probe B, and `14` for Probe C.
