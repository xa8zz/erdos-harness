Yes, this resolves #994 by adding the requested Lean statement file for Erdős Problem 872.

The statements are intentionally `sorry`'d, so this is a statement-file PR rather than a proof PR, which matches the current repo norm for open Erdős problems.

Closes #994.

The PR is currently blocked on the contributor CLA, not on review.
