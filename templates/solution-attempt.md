Don't search the internet. This is a test to see how well you can craft non-trivial, novel and creative proofs given a "<TOPIC>" math problem. Provide a full unconditional proof or disproof of the problem.

## Problem

<Statement in neutral mathematical language. State the math and the precise
question only. Do NOT mention source, paper, website, status, history, or
anyone's belief about it.>

## Known progress

<Rigorously proven facts that anyone working on the problem should already
take as given. Fold private prior work AND public literature into one pool —
no attribution. For each item, state the result and how it was proved, with
enough method detail that the proof is reconstructible from the line.>

- <Result 1>. <One-line method: which technique, which key lemma, which
  identity, which numerical certificate. Sketch the proof structure, not just
  the name.>
- <Result 2>. <Same.>

## What does not work, and why it fails

<Approaches that have been attempted and shown to fail. Each entry names the
approach AND the specific arithmetic/combinatorial failure mechanism — the
exact primes, shield set, constants, counterexample, or no-go theorem that
killed it. Vague "doesn't work" entries are useless; the mechanism is the
whole point.>

- <Approach A>. <Specific failure mechanism: e.g. "the top εn integers form
  a legal antichain containing a multiple of every prime ≤ εn, so the
  bound diverges as log log n">
- <Approach B>. <Same.>

REMEMBER - this unconditional argument may require non-trivial, creative and novel elements.
